# Core
numpy>=1.26.0,<2
opencv-contrib-python>=4.9.0,<4.12

# Raised-hand gesture registration fallback (legacy mp.solutions.pose API).
# Pinned: 0.10.14 still ships mp.solutions; newer wheels (0.10.3x) are Tasks-only.
mediapipe==0.10.14

# Optional YOLO/recovery detection only:
# ultralytics>=8.3.0

# Optional OSNet/ONNX Re-ID backend:
# onnxruntime>=1.17.0
